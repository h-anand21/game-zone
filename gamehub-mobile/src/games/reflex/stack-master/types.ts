// ============================================================
// GameHub — Stack Master Types
// ============================================================

export interface StackBlock {
  id: number;
  x: number; // 0 to 100%
  width: number; // percentage
  color: string;
}
