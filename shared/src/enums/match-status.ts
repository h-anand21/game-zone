// ============================================================
// GameHub — Shared Enums: Match Status
// ============================================================

/** Lifecycle status of a multiplayer match. */
export enum MatchStatus {
  PENDING = 'PENDING',
  ACTIVE = 'ACTIVE',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}
